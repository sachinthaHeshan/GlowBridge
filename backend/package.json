{
  "name": "glowbridge-backend",
  "version": "1.0.0",
  "description": "GlowBridge Beauty Marketplace Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for Node.js backend. Use npm start to run the server.'",
    "build:prod": "npm install --only=production",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "echo 'Linting not configured yet'",
    "analyze-db": "node scripts/analyze-database.js",
    "db:migrate": "echo 'Database migration not configured yet'",
    "db:seed": "echo 'Database seeding not configured yet'"
  },
  "keywords": [
    "beauty",
    "marketplace",
    "api",
    "nodejs",
    "express"
  ],
  "author": "GlowBridge Team",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "node-fetch": "^3.3.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
